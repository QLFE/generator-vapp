import api from '@/api';
import * as types from '../mutation-types';

// initial state
const state = {
};

const mutations = {
};

const actions = {
};

export default {
  state,
  mutations,
  actions
};
